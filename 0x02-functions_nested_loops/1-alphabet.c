#include <stdio.h>
/**
 *main - function to print alphabet
 *Return: end function
 */
int main(void)
{
void print_alphabet(void)
{
	char alphabet = 'a';
	char n = '\n';

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)

putchar(alphabet);
putchar(n);
return;
}
}
